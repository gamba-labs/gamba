import { Card, Cards } from "nextra-theme-docs";
import { projectLinks } from "./exampleLinks";

# 👨‍💻 Examples

Projects built on Gamba

<Cards>
  {projectLinks.map(({ title, href, image }) => (
    <Card
      style={{
        position: "relative",
      }}
      arrow
      image
      title={title}
      target="_blank"
      href={href}
      key={title}
    >
      <div
        style={{
          position: "absolute",
          top: 0,
          left: 0,
          zIndex: 10,
          width: "100%",
          height: "100%",
          backgroundColor: "rgba(0, 0, 0, 0.5)",
          display: "flex",
          justifyContent: "center",
          alignItems: "center",
          color: "white",
          fontSize: "24px",
          overflow: "hidden",
          transition: "background-color 0.3s",
        }}
        onMouseEnter={(e) => {
          e.target.style.backgroundColor = "rgba(0, 0, 0, 0)";
        }}
        onMouseLeave={(e) => {
          e.target.style.backgroundColor = "rgba(0, 0, 0, 0.5)";
        }}
      />
      <div
        style={{
          position: "relative",
          height: "100px",
          backgroundImage: `url(${image})`,
          backgroundSize: "cover",
          backgroundPosition: "center",
          borderRadius: "8px",
          overflow: "hidden",
        }}
      ></div>
    </Card>
  ))}
</Cards>
<Cards
  style={{
    display: "flex",
    justifyContent: "center",
  }}
>
  <Card
    arrow
    image
    title="Add Your Project"
    target="_blank"
    href="https://github.com/gamba-labs/gamba/blob/docs/apps/website/pages/docs/exampleLinks.js"
    key="add-your-project"
    style={{
      width: "33%",
      margin: "auto",
    }}
  />
</Cards>
