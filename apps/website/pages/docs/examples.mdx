import { Cards } from "nextra/components";
import { projects } from "../../components/projects";

# 👨‍💻 Examples

Projects built on Gamba

<Cards>
  {projects.map(({ name, link }) => (
    <Cards.Card key={name} title={name} href={link} target="_blank" />
  ))}
</Cards>

<Cards>
  <Cards.Card title="Add Your Project" href="https://github.com/gamba-labs/gamba/blob/docs/apps/website/components/projects.ts" target="_blank" />
</Cards>
